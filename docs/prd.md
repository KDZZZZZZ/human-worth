# Human Worth 产品需求文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.2 · 2026-09-17 |
| 产品行为基线 | 本文[端到端交互场景](#trusted-scenarios) v1.0，用户明确指定为可信设计 |
| 来源范围 | [用户故事 v0.4 的 Git 历史](https://github.com/KDZZZZZZ/human-worth/blob/839a8502e6df3ed6d15bff2fa95fb2d51bf68fd3/docs/user-stories.md)第 4 节，仅六个端到端场景；长文已删除 |
| 原始场景 SHA-256 | `1b02576ba200b4fba0f5fc233650a53b1c241384426de41c7f62e81c00e38b04` |
| 文档性质 | 统一保存可信场景与派生需求；功能编号、章节组织和验收矩阵为派生整理 |
| 未决事项 | 场景未明确的规则单独列出，不以旧文档其他章节或 agent 推断补成已确认需求 |

本 PRD 使用 S1～S6 对应可信设计的场景一～六，S4.3 表示场景四第 3 步。产品行为以可信设计及后续明确的人类修订为准；本 PRD 不构成对额外技术方案或新增行为的独立批准。

六个场景保留原文、顺序和步骤。来源链接仅用于溯源，其他历史章节不纳入可信范围；后续人类修订应记录版本与依据，并同步相关需求和验收条件。

## 1. 产品概述

平台围绕人类与 agent 的作品比较组织内容。用户从推荐首页发现任务，提交带双方作品的新任务，或向任何公开可见任务追加一方作品。内容审核通过后公开并接受真人投票。

用户也可以跳过投票直接查看统计，代价是永久放弃该账号在该任务上的后续投票权。网站与 MCP 使用相同的资格记录。平台管理员在独立的 Dashboard 中进行日常审核，并启动、管理云端生成者和投票者的挑战流程。

公开看板与热度排行榜位于同一页面，用于观察整体差距及热门任务。推荐首页的排列由平台推荐规则决定，排行榜的排列由热度决定。

## 2. 目标与范围

| 目标 | 用户可观察的结果 | 来源 |
| --- | --- | --- |
| 连续发现任务 | 打开首页即看到平台推荐，滚动加载后续已发布任务 | S1.1～S1.2 |
| 发布有完整对照的任务 | 新任务包含双方作品，通过审核后才公开 | S2.1～S2.4 |
| 开放参与已有任务 | 所有用户可向任意可见任务单方追加，不需要作者批准 | S3.1～S3.3 |
| 集中管理内容与云端挑战 | 管理员在 Dashboard 完成审核、启动、运行管理和异常处理 | S4.1～S4.7 |
| 支持只看结果的使用方式 | 网站及 MCP 可显式读取统计，平台先永久关闭该题后续投票资格 | S5.1～S5.5 |
| 同时观察趋势与热门内容 | 同页展示差距看板和按热度降序的任务榜 | S6.1～S6.3 |

本次范围为六个场景描述的功能与交互。应用技术栈、云端运行环境的搭建、完整接口结构、具体算法阈值及发布安排由后续方案明确；本 PRD 不自动继承旧文档的其他故事或 MVP / V1 划分。

## 3. 用户与权限

| 主体 | 可以完成的事情 | 边界 |
| --- | --- | --- |
| 平台用户，包括任务发布者 | 浏览推荐与任务、提交新任务、向任何可见任务追加作品、按资格投票、显式查看统计 | 不能控制云端挑战；任务作者没有批准他人追加的专有权限 |
| 本地 MCP 用户 | 使用自己的 token 阅读公开内容，显式请求投票统计 | 不代为投稿或投票；读取统计会改变同一账号的该题投票资格 |
| 平台管理员 | 使用管理员 Dashboard 审核、处理举报和下架、启动与管理云端挑战、复盘结果 | 管理入口与公开看板分开；所有管理操作关联管理员身份与结果 |
| 云端生成者 / 投票者 | 在管理员启动的运行中生成作品、学习经验、验证及匿名对抗 | 最终作品仍须审核后公开；云端过程不取代真人投票和系统状态计算 |

来源：S2、S3、S4、S5。具体注册、token 发放及管理员身份授予方式未在场景中定义，见待定项。

**权限相互独立：** 用户失去某题投票权后，仍可追加该题作品；投稿不会恢复投票资格。任务作者和其他普通用户都不能启动、取消、重跑或配置云端挑战。来源：S3.4、S4.3～S4.7。

<a id="trusted-scenarios"></a>
<!-- trusted-scenarios:start -->
## 4. 端到端交互场景

### 场景一：从推荐首页发现任务

1. 用户打开首页，直接进入平台推荐的发现流，向下滚动连续加载已发布任务。
2. 卡片展示任务摘要和允许公开的信息，不预取单题具体投票统计，不影响投票资格。
3. 用户进入任务详情，按自己的资格选择投票，或明确选择查看统计并永久放弃该题后续投票权。
4. 用户进入“看板与排行榜”页面，在同一页查看整体差距和按热度排列的任务。

### 场景二：提交双方作品、审核后发布

1. 发布者填写任务信息，至少提交一件人类作品与一件 agent 作品，可使用文件、压缩包、链接等形式。
2. 在各作品的上传表单中记录来源、相应授权和配置；缺少任一方作品时可以保存草稿，但不能送交发布审核。
3. 任务及双方初始作品一起进入待审核；官方审核通过后公开，驳回则说明原因并允许修改重提。
4. 已发布任务进入推荐候选、公开查询与真人投票；用户提交任务不启动云端 agent。

### 场景三：所有用户向任何可见任务追加作品

1. 任意用户打开任意公开可见任务即可选择追加人类或 agent 作品，无需是任务发布者，无需任务作者邀请或批准，也无需同时提交另一方。
2. 任务可见即表示任务本身已通过审核；追加投稿不会让已有任务重新送审，任务保持已发布状态。
3. 投稿者上传作品并设置该件作品的授权或配置；只有新增作品按自己的发布流程送审，通过后加入公开参选列表，驳回时向投稿者反馈原因。
4. 该用户即使已因查看统计而失去这道题的投票权，也仍可追加作品；投稿不恢复投票资格。MCP 仍只提供内容读取，不代为投稿。

### 场景四：平台管理员通过 Dashboard 日常审核并管理云端挑战

1. **进入每日工作台。** 平台管理员登录管理员 Dashboard，查看待审核任务与作品、举报、云端挑战状态、异常和额度使用情况。该页面与公开的看板 / 排行榜分开，普通用户无管理权限。
2. **处理日常审核。** 管理员审核新任务及双方初始作品、独立追加作品和云端生成作品，执行通过、驳回及原因反馈，按需要处理举报和下架。任何公开可见任务本身已过审，不因新投稿重复审核原任务。
3. **启动云端挑战。** 管理员在 Dashboard 选择已公开可见任务、生成者 / 投票者配置及轮数与预算，确认材料可用后启动。任务作者及其他普通用户均不能执行这一步。
4. **查看运行过程。** Dashboard 展示阶段、轮次、配置和消耗；待定任务只追加生成，agent 领先任务可提炼经验，人类领先任务准备授权材料、验证投票者，再按匿名对抗流程运行。
5. **处理失败与停止。** 模型失败、预算不足或材料失效时，管理员查看原因并按规则取消或重新启动；重新启动创建关联的新运行并重新检查任务、授权和额度。取消或结束后的迟到结果不继续推进，也不登记新作品。
6. **处理产出与发布。** 最终候选进入 Dashboard 的作品审核待办，通过后公开并开放真人投票。登记响应丢失或重试时先找回已有作品及审核状态，避免重复作品和绕过审核。
7. **复盘与后续安排。** 管理员查看运行结果、审核记录和有效真人反馈；系统独立计算任务状态与翻转，管理员据此安排后续挑战。操作身份和结果均留存，普通用户只能查看允许公开的进度。

本场景统一覆盖管理员的日常审核、挑战启动、过程管理、取消、失败处理和登记重试。

### 场景五：通过 MCP 查看统计并永久失去该题投票权

1. 用户通过自己的 token 调用 `list_tasks` 和默认 `get_task` 阅读公开内容，不影响投票资格。
2. 用户明确选择读取该题投票统计，客户端说明永久失去该题投票权的后果后提交显式统计请求。
3. 平台先按账号与任务保存永久资格变化，再返回统计；统计请求本身不提交选票。
4. 该账号之后在网站也不能再投这道题，换 token、换作品对或新增作品均不恢复；其他任务的资格不受影响。
5. 网站用户通过“跳过投票并查看统计”入口执行相同流程。

### 场景六：看板与热度榜同页查看

1. 用户在一个页面看到整体差距看板和热度排行榜；排行榜按热度降序排列已发布任务。
2. 用户可直接浏览平台 / 分类汇总、任务热度与摘要；首页推荐顺序由平台推荐规则决定。
3. 点击任务进入详情；从榜单或看板下钻到该题具体得票和胜率时，仍经过 US-36 的资格变更。

<!-- trusted-scenarios:end -->

## 5. 功能需求

### FR-01 推荐发现首页

来源：S1.1～S1.4。

1. 网站首页直接展示平台推荐的任务发现流，向下滚动连续加载已发布任务。
2. 任务卡片提供标题或摘要及允许公开的信息，用户可以进入任务详情。
3. 卡片和预加载不读取单题具体投票统计，不影响用户的投票资格。
4. 用户在详情中按资格投票，或明确选择查看统计并永久放弃该题后续投票权。
5. 首页可进入看板与排行榜共用的页面。推荐规则的具体算法与参数待定。

### FR-02 提交双方作品并发布新任务

来源：S2.1～S2.4。

1. 创建任务时填写任务信息，至少提交一件人类作品和一件 agent 作品。
2. 作品支持文件、压缩包、链接等交付形式；上传表单记录各作品的来源、相应授权和配置。
3. 缺少任一方作品时可以保存草稿，但不能送交发布审核。
4. 任务与双方初始作品一起进入待审核；管理员审核通过后公开，驳回时给出原因并允许修改重提。
5. 发布后的任务可以进入推荐候选、公开查询与真人投票。用户提交或发布任务不启动云端 agent。

文件白名单、大小限制、授权选项以及配置字段的完整结构未由该场景定值。

### FR-03 所有用户追加任意可见任务

来源：S3.1～S3.4。

1. 任意用户都可以向任意公开可见任务追加人类作品或 agent 作品，无需是发布者、无需作者邀请或批准，也无需同时提交另一方。
2. 可见任务本身已通过审核。追加作品不使任务重新送审，原任务保持已发布状态。
3. 投稿者上传新作品并设置该作品的授权或配置；只有新作品进入自己的发布流程。
4. 新作品审核通过后加入公开参选列表；驳回时向投稿者说明原因。
5. 已因查看统计失去该题投票权的用户仍可追加作品；追加不恢复投票资格。
6. MCP 只提供内容读取，不代为追加投稿。

### FR-04 管理员 Dashboard 与云端挑战管理

来源：S4.1～S4.7。日常审核、启动、取消、失败处理与重试属于同一个管理员工作场景。

#### FR-04.1 每日工作台

- 平台管理员登录独立的管理员 Dashboard，查看待审核任务与作品、举报、云端挑战状态、异常及额度使用情况。
- Dashboard 与公开看板 / 排行榜分开；普通用户没有管理权限。

#### FR-04.2 日常审核

- 审核对象包括新任务及双方初始作品、独立追加作品和云端生成作品。
- 管理员可以通过或驳回并反馈原因，按需要处理举报和下架。
- 公开可见任务本身已经过审，不因追加投稿而重复审核原任务。

#### FR-04.3 启动挑战

- 只有平台管理员可以在 Dashboard 中选择公开可见任务、生成者 / 投票者配置及轮数和预算，确认材料可用后启动。
- 任务作者及其他普通用户不能执行启动操作；用户发布内容不是云端启动入口。

#### FR-04.4 运行观察与分流

- Dashboard 显示当前阶段、轮次、配置和消耗。
- 任务结论待定时，只进行追加生成。
- Agent 领先时，可从已有结果中提炼经验。
- 人类领先时，准备授权材料、验证投票者，再按匿名对抗流程运行。

上述分流沿用可信场景；材料结构、验证门槛和对抗细节需另行明确。

#### FR-04.5 异常、取消与重新启动

- 模型失败、预算不足或材料失效时，管理员可以查看原因并按规则取消或重新启动。
- 重新启动创建关联的新运行，重新检查任务、授权和额度，不覆盖原运行。
- 取消或结束后的迟到结果不能继续推进，也不能登记新的作品。

#### FR-04.6 产出审核与登记重试

- 最终候选进入作品审核待办；审核通过后才能公开并开放真人投票。
- 登记响应丢失或重试时，先找回已有作品及审核状态，避免重复作品和绕过审核。
- 生成完成、作品登记、审核发布和真人反馈分别体现，不能混写为同一个“获胜”结果。

#### FR-04.7 复盘与后续安排

- 管理员查看运行结果、审核记录和有效真人反馈。
- 系统独立计算任务状态和翻转，管理员据此安排后续挑战。
- 记录操作身份和结果；普通用户只能查看允许公开的进度。

### FR-05 MCP 与直接查看统计

来源：S5.1～S5.5，并与 S1.2～S1.3、S3.4、S6.3 一致。

1. 用户使用自己的 token 调用 `list_tasks` 和默认 `get_task` 阅读公开内容，不影响投票资格。
2. 查看单题投票统计须显式提出；客户端先说明永久失去该题投票权的后果，再提交统计请求。
3. 平台先按“账号 × 任务”保存永久资格变化，再返回统计。请求本身不产生任何选票。
4. 此后同一账号在网站也不能再投这道题；更换 token、切换作品对或新增作品均不恢复资格，其他任务不受影响。
5. 网站提供“跳过投票并查看统计”入口，并执行相同流程。
6. 普通读取和显式统计读取须区分，不能把两者都描述为无副作用；统计请求的具体参数与返回结构待定。

| 读取方式 | 用户得到什么 | 对该题投票资格的影响 |
| --- | --- | --- |
| `list_tasks` / 默认 `get_task` | 允许公开的任务内容 | 不改变 |
| 显式请求单题投票统计 | 在说明后果后读取统计 | 先永久关闭该账号的后续投票资格，再返回统计 |
| 网站跳过投票看统计 | 与 MCP 相同的统计访问路径 | 与 MCP 共用永久资格记录 |

### FR-06 看板与热度排行榜同页

来源：S6.1～S6.3，并与 S1.4 一致。

1. 一个页面同时呈现整体差距看板和热度排行榜。
2. 排行榜只排列已发布任务，按热度降序；首页仍由平台推荐规则决定顺序。
3. 用户可直接浏览平台 / 分类汇总、任务热度与摘要。
4. 点击任务进入详情；从榜单或看板查看该题具体得票或胜率时，也必须经过 FR-05 的资格变化流程。
5. 热度计算、推荐规则、统计窗口与刷新频率未在场景中定值；不能自行引入固定权重或胜率排行。

## 6. 状态流转与业务不变量

以下是对场景行为的整理，不指定存储结构或接口实现。

| 对象 | 场景要求的流转或关系 | 来源 |
| --- | --- | --- |
| 新任务及双方初始作品 | 可存草稿 → 双方齐备后送审 → 审核通过公开；驳回后说明原因、允许修改重提 | S2.1～S2.4 |
| 追加作品 | 上传并设置授权 / 配置 → 仅新作品送审 → 通过后公开参选，原任务保持已发布 | S3.1～S3.3 |
| 云端最终作品 | 生成候选 → 管理员审核待办 → 审核通过 → 公开并开放真人投票 | S4.6 |
| 云端运行 | 管理员启动、观察、取消或按规则重启；重启产生关联的新运行，终止后的迟到结果无效 | S4.3～S4.5 |
| 账号在任务上的投票资格 | 满足现有投票条件 → 显式查看统计时永久关闭后续投票资格 → 返回统计 | S5.1～S5.5 |
| 任务对比结论 | 待定 / agent 领先 / 人类领先用于运行分流；系统根据有效真人反馈独立计算状态与翻转 | S4.4、S4.7 |

必须保持的关系：

- **公开可见意味着任务已经过审。** 投稿追加不重审原任务。
- **审核通过与作品胜负分别处理。** 生成或发布完成不替代真人投票及系统结论。
- **投稿权限与投票资格分别处理。** 永久失去某题投票权不阻止追加作品，追加也不恢复资格。
- **内容读取与统计读取分别处理。** 普通阅读不触发资格变化；显式统计读取必须先记录变化再返回结果。
- **任务发布与云端启动分别处理。** 云端执行由平台管理员在 Dashboard 控制。

## 7. 页面与入口

下表整理场景已有入口，不定义额外页面、URL 或具体视觉布局。

| 入口 | 主要内容与动作 | 来源 |
| --- | --- | --- |
| 首页 | 平台推荐的无限发现流；进入任务详情与看板排行页 | S1 |
| 任务详情 | 浏览任务、按资格投票、显式查看统计、所有用户追加作品 | S1.3、S3、S5.5 |
| 创建任务 / 追加作品表单 | 文件、压缩包、链接；来源、适用授权与配置；提交发布流程 | S2、S3 |
| 看板与排行榜页 | 整体 / 分类汇总、热度降序任务榜、任务详情入口 | S6 |
| 管理员 Dashboard | 日常审核、举报、云端挑战启动与管理、异常、额度及复盘 | S4 |
| 本地 MCP | `list_tasks`、默认 `get_task` 及显式统计读取 | S5 |

## 8. 验收矩阵

下列是后续实现应提供的证据要求，**不是已执行的功能测试结果**。当前交付是需求文档。

| 编号 | 前置条件与操作 | 可观察结果 | 所需证据 | 来源 |
| --- | --- | --- | --- | --- |
| AC-01 | 进入首页并连续滚动 | 连续加载推荐的已发布任务，卡片不预取单题统计，资格不变 | 首页真实交互、内容与资格记录 | S1.1～S1.2 |
| AC-02 | 创建任务但缺人类或 agent 一方 | 可保存草稿，不能送审 | 表单结果和任务状态 | S2.1～S2.2 |
| AC-03 | 双方齐备后送审，分别通过或驳回 | 通过才公开；驳回有原因且可修改重提；不启动云端运行 | 内容状态及运行记录 | S2.3～S2.4 |
| AC-04 | 非发布者向任意可见任务只追加一方作品 | 无需作者批准，仅新作品送审，原任务保持已发布 | 不同账号的真实投稿流程及前后状态 | S3.1～S3.3 |
| AC-05 | 已失去该题投票权的用户追加作品 | 投稿仍可进行，不恢复该题投票资格 | 投稿结果与资格记录 | S3.4 |
| AC-06 | 平台管理员与普通用户分别进入管理入口并尝试管理 | 管理员可处理每日待办并控制运行，普通用户不可执行管理动作 | 实际管理入口与权限结果 | S4.1～S4.3 |
| AC-07 | 管理员选择不同结论的任务启动运行 | 按待定、agent 领先、人类领先分流；显示阶段、轮次、配置和消耗 | 对应运行过程记录 | S4.3～S4.4 |
| AC-08 | 模型失败、额度不足或材料失效后取消 / 重启，并产生迟到结果 | 原因可查；重启产生新运行并复核条件；终止运行不继续推进或登记作品 | 异常、关联运行及迟到结果处理记录 | S4.5 |
| AC-09 | 最终候选登记响应丢失后重试 | 找回原作品及审核状态，无重复作品；审核通过才公开投票 | 作品、审核及登记前后状态 | S4.6 |
| AC-10 | 管理员复盘一次产生真人反馈的挑战 | 运行、审核、反馈和系统结论可关联，操作身份与结果留存 | Dashboard 与关联记录 | S4.7 |
| AC-11 | 使用 token 进行默认 MCP 阅读 | 返回公开内容，不提交选票、不改变资格 | MCP 响应与资格前后状态 | S5.1、S5.3 |
| AC-12 | 网站或 MCP 显式查看统计，再换 token / 作品对或等待新增作品后投票 | 先保存永久资格变化再返回统计；同账号仍不可投该题，其他题不受影响 | 网站与 MCP 跨入口、持久化和先后顺序证据 | S5.2～S5.5 |
| AC-13 | 进入看板排行页并查看热门任务 | 同页呈现看板和按热度降序的已发布任务；首页仍按推荐规则排列 | 两个入口的真实展示与排序数据 | S1.4、S6.1～S6.2 |
| AC-14 | 从看板或榜单下钻到某题得票或胜率 | 同样执行先关闭资格再返回统计，不存在旁路 | 下钻结果与同一账号资格记录 | S6.3、S5 |

## 9. 待明确事项

这些问题反映六个场景没有给出的细则。已明确的页面关系、开放追加、审核边界、管理员控制权及永久资格规则不重新作为可选项。

| 问题 | 已有约束 | 尚需明确 |
| --- | --- | --- |
| 任务与作品字段 | 双方首发、单方追加、来源和适用授权 / 配置 | 具体必填字段、文件限制、链接要求、授权默认与撤回策略 |
| 用户身份与 token | MCP 使用用户自己的 token；资格按账号 × 任务记录 | 注册登录、token 发放与吊销、管理员身份授予方式 |
| 真人投票 | 按资格投票，显式看统计后永久失去该题后续资格 | 配对方法、投票选项、重复投票、已有票处理及其他资格条件 |
| 显式统计请求 | 事先说明后果，先保存永久资格变化再返回结果 | MCP 请求形态、返回字段、并发投票与统计读取的具体事务语义 |
| 推荐与热度 | 首页平台推荐；榜单热度降序，任务已发布 | 信号、权重、窗口、刷新、同分顺序与防刷策略 |
| 任务状态与翻转 | 系统依据有效真人反馈独立计算 | 有效票条件、最低票数、领先与翻转判定算法 |
| 云端配置与学习 | 管理员选择角色配置与预算；按任务结论分流 | 模型与服务方案、材料结构、验证阈值、匿名对抗细节 |
| 额度与异常 | 显示消耗，取消后迟到结果无效，重启创建关联新运行 | 消耗单位、预算和轮数默认值、允许重试条件与次数 |
| 审核与下架 | 平台管理员处理，公开可见任务已经过审 | 审核准则、处理时限、下架对既有票数与历史运行的影响 |
| 其他历史需求 | 本 PRD 仅依据被指定的场景模块 | 未在六个场景中完整定义的其他功能，另行取得明确需求依据 |

## 10. 需求追溯

| 可信场景 | PRD 功能 | 验收 |
| --- | --- | --- |
| S1 推荐发现 | FR-01、FR-05、FR-06 | AC-01、AC-12～AC-14 |
| S2 双方首发与审核 | FR-02 | AC-02～AC-03 |
| S3 所有用户追加 | FR-03、FR-05 | AC-04～AC-05 |
| S4 管理员 Dashboard 与云端挑战 | FR-04.1～FR-04.7 | AC-06～AC-10 |
| S5 MCP 与永久资格变化 | FR-05 | AC-11～AC-12 |
| S6 看板热度榜同页 | FR-06、FR-05 | AC-13～AC-14 |

原文中的历史 `US-36` 引用在本 PRD 中由 FR-05 和可信场景 S5 承接，不自动带入原用户故事其他章节的验收细则。
