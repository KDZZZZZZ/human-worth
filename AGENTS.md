# Human Worth 项目协作规则

## 项目目标

我们要做一个可以发帖、评论和投票的网站。用户分享自己认为“人类比 agent 做得更好”的任务，拿出双方的作品或表现，让其他人比较、讨论和投票。

任务可以是代码、写作、绘画、投资，也可以是陪伴家人、享受生活，任何事都可以。评判不必依赖统一分数或量化指标，而是看哪一方更能打动人，由真人自己决定。

所有用户都可以向同一个公开任务追加人类或 agent 作品参选。Agent 作品可以使用不同的模型、提示词、harness、skill 等配置，让同一个任务不断出现新的尝试。

我们希望持续追踪人类和 agent 的差距，让人们意识到人类的价值，也让 agent 从人类的判断中学习。这件事应该让双方都受益、共同进步。

项目统一命名为 **Human Worth（人类价值）**，仓库与工程标识使用 `human-worth`。

## 人类摘要

| 主题 | 项目规则 | 详情 |
| --- | --- | --- |
| 范围 | 本目录及其子目录；需求文档、部署基座与后续产品开发 | [范围与优先级](#scope) |
| 基线 | 公开仓库 human-worth；日常开发从最新 origin/dev 建任务分支 | [Git 与 GitHub](#git-workflow) |
| PR | **只有收到人类明确命令才能创建 PR，含 draft PR**；dev / main 只经 PR，CI 门槛，0 同行审批 | [PR 创建授权](#pr-authorization) |
| 设计 | PRD 统一记录可信场景与派生需求，历史故事仅用于溯源 | [设计治理](#design) |
| 技能 | 编程任务使用 ponytail，优先现有能力与最小可行改动 | [技能路由](#skills) |
| 参考 | 设计不清晰时主动找合适的成熟参考，补全并记录取舍 | [参考与调研](#references) |
| 研究 | 比较模型、提示词或算法时保留可复现的版本树；当前不建空目录 | [研究记录](#research) |
| 验收 | 验证用户目标与关键状态；文档通过检查不等于产品功能已实现 | [充分验收](#acceptance) |

## 当前范围与未定参数

- **已确认治理**：GitHub public 仓库 `KDZZZZZZ/human-worth`；默认 / 日常基线 `dev`；`dev` 和 `main` 只允许 PR，同行审批人数为 0；合入 dev 前通过 CI，合入后自动部署本机，经阿里云 CLI 中已有服务器 IP 提供前端 / API 公网入口。
- **API 环境路由（人类明确要求）**：前端开发调用公网 API，部署后走内部调用。前端统一使用相对 `/api/*`；`npm run dev:frontend` 的本地代理连接公网，部署入口由 Nginx 通过内部隧道转发，不在前端组件中硬编码公网地址。
- **后端目标（人类明确要求）**：分布式 Go 架构，以学习分布式微服务为目标。当前具体设计采用七个业务服务、gateway 与 challenge-worker，见[架构划分](docs/backend-architecture.md)与[部署及实验计划](docs/deployment.md)；服务边界和实验配置属于技术方案。模块设计从 [Identity](docs/backend-identity.md) 开始，七服务 Proto 尚待逐项编写；旧综合后端草稿已按用户要求删除。上述设计不表示业务或集群已经部署。
- **作品与投票（2026-09-18 人类修订）**：作品通过审核即允许展示，不设置独立公开/私有状态或公开授权开关；投票时一次展示该任务全部过审作品，用户从中单选一件最认可的作品。见 [PRD H1](docs/prd.md#human-revisions)。
- **额度展示（2026-09-18 人类修订）**：管理员概览和运行展示不显示额度余额、已用额度或消耗数值，见 [PRD H2](docs/prd.md#hide-quota)；本次修改不取消既有后台预算控制。
- **Google 登录（2026-09-18 人类修订）**：支持 Google 登录，见 [PRD H3](docs/prd.md#google-login)。OpenAPI 已补充登录、回调与退出契约，仍待实现；登录与换凭据不改变稳定账号和原有投票资格。具体 OAuth、Cookie 与建号规则属于技术方案。
- **实现范围**：当前基座使用 Node.js 24 标准库、Python 3 控制器、systemd 与 ECS Nginx / SSH；仅有建设中页面和健康检查，产品业务仍待实现。以上方案为 agent 在用户授权内选定，不是人类指定技术栈。
- **产品参数**：可信场景没有定值的推荐、热度、文件限制、投票阈值等，按下文成熟参考流程自主研究并提出可验证的实现选择；不要伪装为用户已确认，也不要仅因缺少细节就停止工作。

历史授权涵盖创建与配置公开仓库、保护规则、CI、本机部署和公网入口，以及当时完成这些工作的必要推送、PR 与合并验证。**这些历史授权不构成后续任务自动开 PR 的许可；创建 PR 必须有覆盖当前任务的明确人类命令。** 具体规则见 [PR 创建授权](#pr-authorization)。

<a id="scope"></a>
## 范围与优先级

- 本文件适用于项目根目录及其所有子目录的 agent 工作。更近目录的有效 `AGENTS.md` 仅在其声明的范围内覆盖本文件；开始修改前检查指令链。
- 保留与任务无关的用户修改。当前追加交付物包括 GitHub 治理、CI、本机自动部署与公网基座；不将此扩展成整套产品或云端挑战 agent 已实现。
- 在系统与开发者指令允许的范围内，冲突依次按以下依据处理：当前明确的人类指令 → 有效 `AGENTS.md` 链 → 已明确授权的可信设计 → 已确认的任务 / Issue / PR 契约 → 既有兼容性测试 → 当前实现 → 外部参考。
- 人类后续修正覆盖 agent 推断。发现会影响用户行为的冲突时明确指出并解决，不能为了沿用现有实现而静默修改需求。
- 使用中文文档与沟通，保留工具名、协议名及必要代码标识的原文。项目指令采用可被工具识别的标准文件名 `AGENTS.md`。

<a id="expectations"></a>
## 用户期望与授权

每轮持续维护目标、可观察结果、范围、排除项、产品不变量、验收证据、已有授权、偏好、假设和未决事项；无需为此额外创建文件。用户在工作中的补充优先作为当前任务修正处理。

- **Human Design**：只有用户明确提出、明确批准或明确指定为可信的内容，包括上述项目目标、已指定的端到端交互场景及后续明确修订；不包含未经确认的 agent 推断或整份历史用户故事。
- **Agent Self-Claimed**：agent 自行引入的路径、编号、文档组织、技术方案、命名、依赖、测试组织及从参考项目得到的设计。不得把这些改写为“用户已确认”。
- 沉默不代表人类批准，但用户已授权 agent 在设计不清晰时主动查找成熟参考。与明确约束兼容、在任务范围内且可验证的产品细节与实现选择直接推进，记录参考、假设和取舍。仅在参考仍无法消除实质歧义，或存在未授权的不可逆影响 / 显著费用时澄清；不要把每个 API、依赖或架构选择都升级为审批。
- 既有明确授权仅在其范围内持续有效。日常开发请求授权完成本地改动与必要验证，不自动授权创建 PR；必须遵守下文的明确人类命令门槛。已有覆盖当前任务的明确开 PR 命令时，无需重复询问，但不能扩展到其他任务。
- 输出交付物时对照目标说明实际完成、验证范围、推断和剩余问题；不把设计文档描述成已运行的功能。

<a id="git-workflow"></a>
## Git 与 GitHub

<a id="pr-authorization"></a>
### PR 创建授权（强制）

**只有收到人类明确要求开 PR 的命令，才能创建 PR。** 此规则适用于任务 PR、发布 PR 和 draft PR，也适用于 CLI、GitHub API、网页或其他工具的创建操作。

- 创建前必须能指出覆盖当前任务的具体人类命令，例如“为这次修改开 PR”或“创建 PR 到 dev”；按命令限定的变更范围和目标执行。
- “实现功能”“修复问题”“生成文档”“按项目流程完成”等普通任务指令，以及用户沉默、CI 通过、0 同行审批或其他任务的历史授权，都不能代替开 PR 命令。
- 没有明确开 PR 命令时，先完成可审阅的本地改动与必要验证，报告结果并等待人类命令；不得自行创建 draft PR 作为等待批准的方式，也不得借机推送、合并或发布。
- 此门槛优先于 README、[分支管理](docs/development.md)及历史记录中“日常开发自动完成 PR / 合并 / 部署”或概括性授权的表述。后续合并与发布仍须符合有效授权范围和下述技术门槛。

### 分支与合并规则

仓库为 [KDZZZZZZ/human-worth](https://github.com/KDZZZZZZ/human-worth)，默认分支与日常集成基线均为 `dev`。完整规则见 [分支管理](docs/development.md)，机器配置见 `ops/github/`。

- 日常工作先检查 `git status --short`、当前分支、远端和近期历史，再从最新 `origin/dev` 创建 `frontend/<type>-<topic>` 或 `backend/<type>-<topic>`；收到明确开 PR 命令后，一条任务分支对应一个 PR。
- 前缀按工作范围选择：`frontend` 用于页面、组件、交互和前端工程；`backend` 用于 API、数据、权限、MCP、云端调度及部署。全局工程、CI 和项目治理默认归 `backend`；跨两端的同一交付按主要变更选前缀，并在 PR 中列清两端范围。不添加作者、账号或 agent 工具名前缀。
- 类型使用 `feat`（新功能）、`fix`（修复）、`refactor`（重构）、`perf`（性能）、`style`（代码格式）、`test`（测试）、`docs`（文档）、`build`（构建 / 依赖）、`ci`（CI/CD）、`chore`（其他维护）、`revert`（回滚）。主题使用小写英文、数字及单连字符，简短说明具体任务；完整定义和例子见 [分支命名规则](docs/development.md#branch-naming)。CI 校验面向 dev 的任务分支名称。
- `dev`：只能经任务 PR **squash merge**；必须在最新目标基线上通过 `CI`。合入后 push CI 再次通过，本机自动部署该 SHA 并检查健康状态。
- `main`：只接受本仓库 `dev` 发起的发布 PR，使用 **merge commit**，同样通过 CI。main 独有发布合并提交不回灌 dev；main 不接受独立开发或补丁。
- 两条分支审批数为 **0**，不要求同行、CODEOWNERS 或最后推送人以外的审批；这只是 GitHub 同行审批门槛，不替代人类开 PR 命令。有合并权限的作者在有效授权范围内、检查通过后可合并。
- 两条分支禁止直接 push、force push 和删除；无绕过名单，不使用 `gh pr merge --admin` 或临时关闭保护。
- 合并前核对 PR 的目标、当前 head SHA、CI 结果和非 draft 状态；CLI 合并使用 `--match-head-commit`。缺陷 / 回滚也经任务分支、CI 与 PR；长驻分支不 reset 或直接 cherry-pick。
- 任务分支可 merge 最新 dev；个人独占分支可 rebase 后 `--force-with-lease`，共享分支不擅自改写历史。main / dev 永不 rebase。
- 保留脏工作区，必要时 worktree 隔离；分支不夹带无关清理。完成合并或用户明确放弃后才删除任务分支，长驻分支保留。
- 仅空仓库第一次建立共同根提交有初始化直接推送；该步骤在启用保护前已完成，不能作为后续例外。

### PR 作者归属

PR 正文以问题及最终行为开头，并使用以下小节：

```markdown
## Human Design

- 只列用户明确提出或批准的决定。

## Agent Self-Claimed

- 列 agent 自行引入的实现、结构、依赖、测试和文档决定；没有则写 None。

## Validation

- 列实际命令、退出码、结果、证据和未完成的必要验证。
```

归属不明确的决定放入 Agent Self-Claimed；不以 `misc`、`chore` 或“顺便”掩盖新增范围。

<a id="design"></a>
## 设计治理

### 依据与可信范围

产品设计统一维护在 [PRD](docs/prd.md)，不单独设置 design 文档路径。其中的[端到端交互场景](docs/prd.md#trusted-scenarios)是用户明确指定的可信设计，保留六个场景原文和历史来源。

PRD 的功能需求与验收条件依据六个场景整理，使用 S1～S6 追溯来源，并把未明确的细则列为待定；不能把文档组织方式或待定项说成新的人类批准。用户故事长文已按用户要求删除，历史其他章节不作为新增需求依据。

PRD 中的可信场景应保留原文及来源信息。场景中的历史故事编号只用于追溯，不能借该编号把原文其他章节的细则自动提升为已确认需求。每项功能及验收条件应标注对应场景，新增组织方式、编号和未决问题属于文档整理，不是新增产品批准。

| 范围 | 治理依据 | 状态 | 使用时机 |
| --- | --- | --- | --- |
| 产品流程与 UI | PRD 场景 S1～S6、功能需求、页面与入口 | 场景由人类明确指定为可信；PRD 不扩大行为范围 | 首页发现、投稿、管理员 Dashboard、统计查看、看板排行变更 |
| 架构与模块 | PRD 场景中的角色边界与交接、[架构划分](docs/backend-architecture.md)、[Identity 设计](docs/backend-identity.md) | Go 与分布式学习为人类目标；七服务边界、数据归属和协作协议为技术方案 | 云端交互、模块职责或调用方向变化 |
| 公开 API / MCP | PRD 场景 S3、S5 及 FR-05、OpenAPI、各模块接口设计 | 读取用途、token 身份及统计副作用明确；HTTP 为草案，独立 Proto 文件待编写 | MCP 能力、统计访问或投稿入口变化 |
| 数据 | PRD 场景 S2～S5、状态流转与业务不变量、各模块核心对象设计 | 发布状态与账号 × 任务资格明确；具体存储和事务协议为技术提案 | 投稿、审核、资格持久化、重试与运行记录变化 |
| 安全与权限 | PRD 场景 S3～S5、用户与权限 | 管理员权限及作品授权边界明确；尚无详细安全方案 | 身份、材料授权、管理操作或资格检查变化 |
| 测试与验收 | PRD 的可信场景与验收矩阵、本文件 | 场景可作为判断依据；无现成测试框架 | 文档核对与后续功能实现 |
| 研究 | 本文件研究记录规则 | agent 提出的工作约定；无现成研究制品 | 模型、提示词、验证或排序算法实验 |
| 发布与 GitHub | [分支规则](docs/development.md)、[部署](docs/deployment.md)、ops/github 与 ops/systemd | 用户明确治理目标，具体方式由 agent 落实 | 推送、合并、部署或发布前 |

### 必须保持的产品边界

1. 首页直接进入平台推荐的无限发现流；看板与按热度排序的排行榜同页。
2. 新任务至少带一件人类和一件 agent 作品，审核后才公开；所有用户均可向任何可见任务追加一方作品，无需任务作者批准。
3. 任务可见即已过审，追加不重审原任务；新增作品和云端产出各自经过审核，通过即允许展示，不另设公开/私有或公开授权开关。
4. 管理员 Dashboard 与公开看板分开；只有平台管理员能启动、取消、重跑和配置云端挑战。
5. 运行终止后的迟到结果不继续推进或登记作品；登记重试先找回已有结果，避免重复投稿或绕过审核。
6. 普通读取不预取具体投票统计。显式查看统计先永久关闭该账号在该任务上的后续投票资格，再返回统计；网站与 MCP 共用记录。
7. 换 token、刷新候选列表或新增作品不恢复该题投票资格，其他任务不受影响；失去投票权不妨碍追加作品。
8. MCP 只承担内容读取，不代为投稿或投票，也不控制云端；显式统计读取必须披露永久资格变化这一副作用。
9. 投票一次展示该任务全部过审且允许展示的作品，用户单选一件最认可的作品；不抽样配对，不采用 left/right/tie 选项。

未由可信场景定义的参数、方案和完整 API 契约不得伪装成已批准设计。需要新增治理文档时先检查当前任务的授权；文档修改授权不包含创建 PR，仍须遵守 [PR 创建授权](#pr-authorization)。

<a id="skills"></a>
## 技能路由

| 任务 | 已验证技能 | 触发条件 | 读取位置 |
| --- | --- | --- | --- |
| 编写、修复、重构、评审代码及选择依赖 | ponytail | 编程任务默认 full；用户点名时加载 | [本机 SKILL.md](/home/oops/.codex/skills/ponytail/SKILL.md) · [固定版本来源](https://github.com/DietrichGebert/ponytail/blob/e3ba2aa6f1e6f0bc4d69eb09c9f0d0a93af56156/skills/ponytail/SKILL.md) |

- 使用前完整读取实际技能文件；路径失效时先查找安装位置或固定版本来源，不虚构已加载。当前项目技能清单仅保留 ponytail。
- 先理解真实调用流程，再优先复用现有代码、标准库和平台原生能力，避免预留抽象、无必要依赖和无关改造。
- 简化不能删除明确需求、信任边界校验、权限控制、避免数据丢失的错误处理或必要验证。用户明确要求高于技能建议；已有授权不重复审批。

<a id="references"></a>
## 参考与调研

**当人类设计不清晰时，必须主动寻找合适的成熟参考，补全可验证的设计后继续工作。** 不把细节缺失默认当作阻塞或审批请求，也不机械照搬参考产品。

调查顺序：会话与期望 → PRD 中的可信场景与需求 → 既有测试、接口、代码与历史 → 已记录参考 → 官方规范、上游代码与成熟产品。时效性事实使用当前权威来源核实。

1. 写清需要解决的具体缺口及不能违反的产品边界。
2. 选取解决相似问题且证据充分的成熟实现；必要时比较多个候选、真实代码和测试。
3. 记录链接、版本 / commit、适配点、差异、采用理由与验收判据。由参考推导的行为标为 Agent Self-Claimed，不改写可信原文。
4. 在授权范围内选择兼容、可逆、维护成本合理的方案并完成验证；避免给人类无必要的选型问卷。
5. 只有研究后仍存在实质歧义、与明确要求冲突或超出已有授权时，提出具体问题并继续不依赖答案的工作。

已核实来源、适用模块及取舍见 [成熟参考](docs/references.md)。当前选用 GitHub 官方 rulesets / Actions、OpenGitOps、systemd、OpenSSH、Nginx 和阿里云 ECS 官方资料；它们支撑工程治理与部署，不自动决定产品投票、审核和排行语义。新方案补充该记录；证据不足时如实标记，不以“能跑”替代“适合”。

<a id="research"></a>
## 研究记录

当前没有实验代码或结果目录。若后续开展模型、提示词、偏好验证、推荐 / 热度算法或其他方案比较，保存逻辑版本树，例如 `R0 基线 → H1 假设 → V1 / V2 实验 → S1 综合结论`，不要求每个节点对应长期 Git 分支。

每个重要节点记录：唯一 ID 与父节点、问题 / 假设 / 相对变化、数据和依赖 / 模型 / 参数 / 环境版本、可恢复代码或补丁 / commit / 制品、准确命令与条件、原始结果位置、证据结论、局限及后续分支；状态使用 `candidate`、`accepted`、`rejected`、`inconclusive` 或 `superseded`。

实验必须从明确父节点派生；保留能排除假设的失败，避免同时改变多个关键变量，只比较条件兼容的结果。代码与原始结果保存在聊天外，大制品记录哈希与取回方式；不保存密钥或未经授权的作品正文。新结果取代旧结论时保留历史，综合节点说明选中与排除的分支。

尚无既定制品位置，可在真实研究任务中采用 `research/<topic>/` 下的说明、版本树、代码、结果和制品目录。这是 Agent Self-Claimed 的默认建议；本轮不创建空研究目录。

<a id="acceptance"></a>
## 充分验收

**验收通过应使人对用户目的已经达成有较高信心，不能只证明能编译或单个样例能运行。**

每项工作建立与风险匹配的矩阵：

| 用户目标 | 前置条件 | 输入 / 动作 | 可观察结果 | 所需证据 | 失败含义 |
| --- | --- | --- | --- | --- | --- |
| 文档忠实于场景 | 已明确可信范围 | 提取、整理、核对 | 原文不失真，需求可追溯，无暗增行为 | 原文比对、链接和编号检查 | 需求污染或遗漏 |
| 发布与开放投稿 | 有任务 / 作品状态和不同账号 | 双方首发、任意用户追加 | 缺一方不送审；追加不重审任务；新作品独立审核 | 后续真实入口及状态记录 | 权限或发布流程错误 |
| 统计与永久资格 | 账号已识别，网站和 MCP 可访问 | 显式看统计，再尝试投票 | 先落资格变化，再返回结果；换 token / 刷新候选列表仍不可投 | 后续跨入口及持久化证据 | 统计泄露或资格可绕过 |
| 管理员独占云端 | 有普通用户与管理员 | 启动、取消、重跑和登记重试 | 权限正确，终止后迟到结果无效，作品不重复 | 后续权限、恢复和真实交互证据 | 未授权执行或重复作品 |

- 从人类要求、可信设计、官方规范或已有兼容契约取得判定依据，不以当前实现自证正确。
- 覆盖适用的正常路径、边界、非法输入、否定行为、状态持久化、并发与恢复、接口和用户可见结果、相邻回归，以及安全、性能和额度限制。只运行与本次变更相关且必要的检查。
- 缺陷修复的证据必须能识别旧缺陷；必要的真实集成不能以只验证内部调用的 mock 替代。纯文档变更不强制新增软件测试或安装检查工具。

### 当前可用的检查与部署验收

- `npm ci --ignore-scripts`：按 lockfile 安装；当前无第三方运行时依赖。
- `npm run ci`：可信场景快照与本地链接、PR 方向及任务分支命名、Node 语法、实际 HTTP 前端 / API 测试、部署 CI 门槛及归档边界测试。GitHub 必需检查名为 `CI`。
- `npm start`：本机基础服务，默认 `127.0.0.1:18090`；只实现建设中页面与 `/api/health`。
- `gh pr checks <编号>` 与 GitHub rulesets API：核实 PR 检查及远端真实规则，不能只看配置文件。
- 合入 dev 后检查 push CI、`human-worth-deploy` 日志和公网 `/api/health`，返回 revision 必须等于预期 dev SHA。命令见 [部署文档](docs/deployment.md)。

变更 CI、部署或公开权限时，检查允许路径和拒绝路径、失败恢复和相邻服务不受影响；只做静态 YAML 检查不能称自动部署已验收。后续产品开发须补充对应真实业务测试，现有基座 CI 不证明 PRD 功能已完成。

### 结论门槛

标记 PASS 前回答：是否仍有现实路径绕过用户目标；是否经过真实入口和关键组件；判据是否独立；证据是否能发现目标缺陷；是否覆盖相关条件而非单例；是否检查可见结果；是否覆盖相邻回归；环境是否有代表性；mock 是否可能掩盖集成失败；研究能否复现。

- **PASS**：准确命令、退出码、结果与必要制品支持声明范围内的关键目标。
- **FAIL**：指明不满足的要求、证据及影响。
- **INCONCLUSIVE**：必要门槛无法验证且替代证据不足时，列缺口、已尝试方案和重跑条件，不声称通过。

本轮可对文档、仓库规则、基础服务和部署链路按实际证据作结论；尚未实现的产品功能不能给出 PASS。研究还需预先明确问题与指标，保存同条件基线、原始结果及可恢复代码，并让结论受证据边界约束。
